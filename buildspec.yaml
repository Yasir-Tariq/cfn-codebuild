version: 0.2
env:
  variables:
    # INSTANCE_TYPE: "t2.micro"
    # KEY_NAME: "yasirKP-ohio"
    # AMI_ID: "ami-0fc20dd1da406780b"
    TAG_NAME: "cfn-cp3"

phases:
  build:
    commands:
      - echo '{"ParameterKey":"'$yasir'"}' | jq --arg ParameterKey ParameterValue '. + {ParameterValue:"'$yasir2'"}' >output.json
      - cat output.json   
artifacts:
  files:
    - output.json